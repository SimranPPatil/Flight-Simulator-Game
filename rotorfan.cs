using UnityEngine;
using System.Collections;

public class rotorfan : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        transform.Rotate(0, 5000, 0);
	}
}
